---

---
<article  id="about" class=" box core_section center bg-slate-100">
  <section class="title center bg-slate-200">
    <img  src="nlogo2.webp" alt="hero logo" width="125px" height="125px"/>
  <h2 class="h2Text">Who is IO Forge?</h2>
  </section>
  <section class ="center text about_container bg-slate-200">
    <p class="pText">A veteran owned company that builds customized software for government contractors and businesses to increase automation, efficiency, and production.
    </p>
    <p class="pText">We specialize in data science, by creating tools that analyze inputs such as necessary resources, labor force, and assets. Passing the inputs through our system, 
    turns the data into simple outputs for your business needs.
    </p>
    <p class="pText">These tools can be used to automate some of your company's workload and helps you find inefficiencies in your business algorithm.
    We can also build a web portal for remote access, so any of your workforce can access this information instantly.</p>
    <p class="pText">On top of all that our dedicated team of programmers can build your business a website or an app for your business. We can integrate database-driven machine learning
      algorithms for your users and business operations.
    </p>
  </section>
</article>
<style>
  .about_container{    
    padding:5%;
    --tw-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
  --tw-shadow-colored: 0 10px 15px -3px var(--tw-shadow-color), 0 4px 6px -4px var(--tw-shadow-color);
  box-shadow: var(--tw-ring-offset-shadow, 0 0 #0000), var(--tw-ring-shadow, 0 0 #0000), var(--tw-shadow);
  border-radius:10px;
  }
  .text{
  
    text-align: left;
	  line-height: 2;
    /* font-size:20px; */
  }
  .title{
    
    font-size:40px;
    color:rgb(100, 100, 100);
    font-weight:bold;
    --tw-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
  --tw-shadow-colored: 0 10px 15px -3px var(--tw-shadow-color), 0 4px 6px -4px var(--tw-shadow-color);
  box-shadow: var(--tw-ring-offset-shadow, 0 0 #0000), var(--tw-ring-shadow, 0 0 #0000), var(--tw-shadow);
  border-radius:10px;
  
  margin-bottom:10%;
  }
 
  h2{
    --tw-drop-shadow: drop-shadow(0 10px 8px rgb(0 0 0 / 0.04)) drop-shadow(0 4px 3px rgb(0 0 0 / 0.1));
  filter: var(--tw-blur) var(--tw-brightness) var(--tw-contrast) var(--tw-grayscale) var(--tw-hue-rotate) var(--tw-invert) var(--tw-saturate) var(--tw-sepia) var(--tw-drop-shadow);
  }
 

</style>